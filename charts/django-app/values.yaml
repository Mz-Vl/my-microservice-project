image:
  repository: 224751578050.dkr.ecr.us-east-1.amazonaws.com/sshevche-repo
  tag: latest
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 8000

ingress:
  enabled: false

config:
  POSTGRES_HOST: db
  POSTGRES_PORT: 5432
  POSTGRES_USER: django_user
  POSTGRES_DB: django_db
  POSTGRES_PASSWORD: pass3254aa


postgresql:
  enabled: true
  global:
    postgresql:
      auth:
        postgresPassword: pass3254aa
        username: django_user
        database: django_db
        password: pass3254aa
  primary:
    persistence:
      size: 2Gi